extends layout

block content
  h1= title
  - const starbucks = ['coffee_type','quantity','cost']
  - let results;
  script.
    function buy(x){
      alert('Purchased Item: ' + x)
   } 
  div(class='container-fluid')
    div(class='row')
      each results, index in starbucks
        div(class='col-sm-2' onclick='buy('+index+')') #{results}